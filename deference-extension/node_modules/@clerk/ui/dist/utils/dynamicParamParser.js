//#region src/utils/dynamicParamParser.ts
const createDynamicParamParser = ({ regex }) => ({ urlWithParam, entity }) => {
	const match = regex.exec(urlWithParam);
	if (match) {
		const key = match[1];
		if (key in entity) {
			const value = entity[key];
			return urlWithParam.replace(match[0], value);
		}
	}
	return urlWithParam;
};

//#endregion
export { createDynamicParamParser };
//# sourceMappingURL=dynamicParamParser.js.map